services:
  postgres:
    image: postgres:16
    container_name: polixis-company-search-db
    environment:
      POSTGRES_DB: company_search
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: